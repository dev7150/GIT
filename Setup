

git init --locally initialize git
git clone <html> --to clone a remote repo

git add <filename>  --add to the staging area
git commit -m "message" --commit to the 

git config --global user.name "Dev"
git config --global user.email "devashish16@gmail.com"

get remote add GIT <html>
git remote remove GIT

git remote set-url origin https://github.com/your-other/repository

git push --set-upstream origin master 
‍

git pull --allow-unrelated-histories GIT master

git push -u GIT master
